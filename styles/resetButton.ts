import css from 'styled-jsx/css'

const resetButton = css`
  button {
    background: none;
    border: none;
    padding: 0;
    color: inhetit;
    font: inherit;
    cursor: pointer;
  }
`

export default resetButton
